library;

export 'src/room_commands.dart';

/// Foundation-level protocol metadata shared across authority boundaries.
abstract final class ProtocolFoundation {
  static const int inputHashVersion = 1;
}
